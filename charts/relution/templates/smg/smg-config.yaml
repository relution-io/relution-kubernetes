kind: ConfigMap
apiVersion: v1
metadata:
  name: {{ include "relution.fullname" . }}-smg-config
  labels:
    {{- include "relution.labels" . | nindent 4 }}
data:
  application.yml: |
    {{- toYaml .Values.smg.app.config | nindent 4 }}
