{{- if .Values.smg.relution.secrets }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "relution.fullname" . }}-smg-secrets
  labels:
    {{- include "relution.labels" . | nindent 4 }}
type: Opaque
data:
  {{- range $key, $val := .Values.smg.relution.secrets }}
  {{ $key }}: {{ $val | b64enc }}
  {{ end -}}
{{- end }}
